export enum OAuthStatus {
    PENDING = "pending",
    SUCCEED = "success",
    FAILED = "failed"
}

export enum OtpVerificationType {
    SIGN_IN = 0,
    SIGN_UP = 1,
    FORGOT_PASSWORD = 2,
    CHANGE_PASSWORD = 3
}